apiVersion: cluster.redpanda.com/v1alpha2
kind: NodePool
metadata:
  name: rp-eu
  namespace: redpanda
spec:
  clusterRef:
    group: cluster.redpanda.com
    kind: StretchCluster
    name: redpanda
  replicas: 1
  image:
    repository: redpandadata/redpanda
    tag: v26.1.6
  services:
    perPod:
      remote:
        enabled: true
